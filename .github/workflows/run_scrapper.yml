name: Run IMPDS Scraper

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:
    inputs:
      year:
        description: 'Year'
        default: '2023'
      month:
        description: 'Month'
        default: '4'
      mode:
        description: 'Scrape mode: all districts per state, or just one'
        type: choice
        options:
          - all
          - one
        default: 'all'

jobs:
  scrape:
    strategy:
      fail-fast: false
      matrix:
        include:
          - start: 0
            end: 9
          - start: 9
            end: 18
          - start: 18
            end: 26
          - start: 26
            end: 99

    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4        # ✅ v4 already Node 24 compatible

      - name: Set up Python
        uses: actions/setup-python@v5   # ✅ v5 already Node 24 compatible
        with:
          python-version: '3.10'

      - name: Install Chrome
        uses: browser-actions/setup-chrome@v2
        id: setup-chrome                                      # ← needs this id
        with:
          chrome-version: '149'  
          install-chromedriver: true


      - name: Install dependencies
        run: pip install selenium

      - name: Run Scraper
        env:
          CHROME_PATH: ${{ steps.setup-chrome.outputs.chrome-path }}
          CHROMEDRIVER_PATH: ${{ steps.setup-chrome.outputs.chromedriver-path }}
        run: |
          python impds_scraper_sql_3.py \
            --start ${{ matrix.start }} \
            --end ${{ matrix.end }} \
            --year ${{ github.event.inputs.year || '2023' }} \
            --month ${{ github.event.inputs.month || '4' }} \
            --mode ${{ github.event.inputs.mode || 'all' }}

      - name: Upload DB chunk
        uses: actions/upload-artifact@v4   # ✅ v4 already Node 24 compatible
        with:
          name: db-${{ matrix.start }}-${{ matrix.end }}
          path: "*.db"

  merge:
    needs: scrape
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Download all DB chunks
        uses: actions/download-artifact@v4   # ✅ v4 already Node 24 compatible
        with:
          path: dbs/

      - name: Merge DBs
        run: |
          python merge.py \
            ${{ github.event.inputs.year || '2023' }} \
            ${{ github.event.inputs.month || '4' }} \
            ${{ github.event.inputs.mode || 'all' }}

      - name: Commit and push final DB
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          git add *_FINAL_*.db || echo "No DB generated"
          git commit -m "Auto-updated scraped district data [skip ci]" || exit 0
          git push